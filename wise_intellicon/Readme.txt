**파인튜닝된 버트의 encoder를 활용하여 sentence-wise attention모델 활용

insu_wise와 labor_wise로 main파일 구성

1. input
 - sentence bert encoder에 해당하는 bert encoder 파일을 입력
 - 근로와 인수계약서 두개 있음

2. output
 - sentence wise attenion 모델의 weight에 해당하는 파일 저장
 - 분류 결과 엑셀 파일 저장